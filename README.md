# ✅ FastAPI Todo App

This project is a **Todo Management REST API** built with **FastAPI** and **SQLite (Todo.db)**.  
It allows you to create, read, update, and delete (CRUD) todo items through clean HTTP APIs.

---

## 🎯 Project Overview

- Backend completely built using FastAPI.
- Data is stored in a local SQLite database (`Todo.db`).
- Interactive API documentation is available via Swagger UI.
- Simple, clean structure that can be extended into a larger production backend.

---

## 🧩 Main Features

- ✅ Create new todo items  
- ✅ Get all todos or a single todo  
- ✅ Update existing todos  
- ✅ Delete todos  
- ✅ SQLite database integration  
- ✅ Automatic interactive docs at `/docs`

---

## 🛠 Tech Stack

- **Language:** Python  
- **Framework:** FastAPI  
- **Database:** SQLite (`Todo.db`)  
- **Server:** Uvicorn  

---

## 📂 Project Structure (high level)

- `main.py` – Entry point of the FastAPI application (routes start here)  
- `app/` – Internal project code (business logic, models, etc.)  
- `Todo.db` – SQLite database file  

